import { Arguments } from "../args/argsParser"
import { GitTemplateRepositories } from "../git/gitClient"

const FILES_TO_REPLACE = [
    'package.json',
    'vite.config.js',
    'vite.config.ts',
]

const replaceTemplateName = (choises: Required<Arguments>, d) => {

}

export default replaceTemplateName